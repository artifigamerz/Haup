{
  "buildCommand": "pnpm install && pnpm --filter fitness-arena run build",
  "outputDirectory": "fitness-arena/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
